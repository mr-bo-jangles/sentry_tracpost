# sentry_tracpost
A plugin for [Sentry](https://www.getsentry.com/) that logs errors to a Trac Install.
## Installation
`$ pip install sentry_tracpost`

And with the new plugin API, senty_tracpost plugin will be added in your sentry server.
